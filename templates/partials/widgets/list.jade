div.Title {!= settings.title !}
ul
  {! for (var i = 0; i < data.length; i++) { !}
  li 
    span.time {!= new Date(data[i].timestamp).toString(settings.ui.dateFormat) !}
    span.name(title="{!= data[i].output !}") {!= data[i].output !}
  {! } !}
